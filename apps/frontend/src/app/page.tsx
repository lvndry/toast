import LandingPage from '@/app/(landing)/LandingPage';

/**
 * The main page component that renders the LandingPage component.
 *
 * @returns {JSX.Element} The rendered LandingPage component.
 */
export default function Page() {
  return <LandingPage />;
}
